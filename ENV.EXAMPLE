# Server
PORT=4000
HOST=0.0.0.0
# Set to your mobile/web dev origin, or leave empty to allow all in dev
CORS_ORIGIN=http://localhost:5173

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=postal_corp
DB_DIALECT=mysql

# JWT (RS256)
# Paste raw PEM or base64-encoded PEM for the private/public keys.
# If base64-encoded, the server will decode automatically.
# Example raw (do NOT commit real keys):
# JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
# JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=